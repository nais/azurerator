
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: azureadcredentials.nais.io
spec:
  group: nais.io
  names:
    kind: AzureAdCredential
    listKind: AzureAdCredentialList
    plural: azureadcredentials
    shortNames:
    - azuread
    singular: azureadcredential
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: AzureAdCredential is the Schema for the azureadcredentials API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: AzureAdCredentialSpec defines the desired state of AzureAdCredential
          properties:
            configMapName:
              description: ConfigMapName is the name of the resulting ConfigMap resource
                to be created
              type: string
            logoutUrl:
              description: LogoutUrl is the URL where Azure AD sends a request to
                have the application clear the user's session data. This is required
                if single sign-out should work correctly. Must start with 'https'
              type: string
            preAuthorizedApplications:
              items:
                description: AzureAdPreAuthorizedApplication describes an application
                  that are allowed to request an on-behalf-of token for this application
                properties:
                  clientId:
                    type: string
                  name:
                    type: string
                type: object
              type: array
            replyUrls:
              items:
                description: AzureAdReplyUrl defines the valid reply URLs for callbacks
                  after OIDC flows for this application
                properties:
                  url:
                    type: string
                type: object
              type: array
            secretName:
              description: SecretName is the name of the resulting Secret resource
                to be created
              type: string
          required:
          - configMapName
          - secretName
          type: object
        status:
          description: AzureAdCredentialStatus defines the observed state of AzureAdCredential
          properties:
            applicationObjectId:
              description: ApplicationObjectId is the Azure AD Application object
                ID
              type: string
            certificateKeyId:
              description: CertificateKeyId is the certificate ID for the latest valid
                certificate credential
              type: string
            clientId:
              description: ClientId is the Azure application client ID
              type: string
            passwordKeyId:
              description: PasswordKeyId is the key ID for the latest valid password
                credential
              type: string
            provisionHash:
              description: ProvisionHash is the hash of the AzureAdCredential object
              type: string
            provisionState:
              description: ProvisionState is a one-word CamelCase machine-readable
                representation of the current state of the object
              enum:
              - New
              - Rotate
              - Retrying
              - Provisioned
              type: string
            provisionStateTime:
              description: ProvisionStateTime is the last time the state transitioned
                from one state to another
              format: date-time
              type: string
            servicePrincipalObjectId:
              description: ServicePrincipalObjectId is the Azure AD Service Principal
                object ID
              type: string
            upToDate:
              description: UpToDate denotes whether the provisioning of the AzureAdCredential
                has been successfully completed or not
              type: boolean
          required:
          - applicationObjectId
          - certificateKeyId
          - clientId
          - passwordKeyId
          - provisionState
          - servicePrincipalObjectId
          - upToDate
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
