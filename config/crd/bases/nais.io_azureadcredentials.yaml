
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: azureadcredentials.nais.io
spec:
  group: nais.io
  names:
    kind: AzureAdCredential
    listKind: AzureAdCredentialList
    plural: azureadcredentials
    shortNames:
    - aad
    singular: azureadcredential
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: AzureAdCredential is the Schema for the azureadcredentials API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: AzureAdCredentialSpec defines the desired state of AzureAdCredential
          properties:
            preAuthorizedApplications:
              items:
                description: AzureAdPreAuthorizedApplication describes an application
                  that are allowed to request an on-behalf-of token for this application
                properties:
                  clientId:
                    type: string
                  name:
                    type: string
                type: object
              type: array
            replyUrls:
              items:
                description: AzureAdReplyUrl defines the valid reply URLs for callbacks
                  after OIDC flows for this application
                properties:
                  url:
                    type: string
                type: object
              type: array
          type: object
        status:
          description: AzureAdCredentialStatus defines the observed state of AzureAdCredential
          properties:
            certificateKeyId:
              description: CertificateKeyId is the certificate ID for the latest valid
                certificate credential
              type: string
            conditions:
              description: Conditions lists the latest available observations of the
                object's current state
              items:
                properties:
                  lastHeartbeatTime:
                    description: LastHeartbeatTIme is the last time we got an update
                      on a given condition
                    format: date-time
                    type: string
                  lastTransitionTime:
                    description: LastTransitionTime is the last time the condition
                      transit from one status to another
                    format: date-time
                    type: string
                  message:
                    description: Message is a human-readable message indicating details
                      about last transition
                    type: string
                  reason:
                    description: Reason is a one-word CamelCase reason for the condition's
                      last transition
                    type: string
                  status:
                    description: Status is the status of the condition, one of True,
                      False, Unknown
                    enum:
                    - "True"
                    - "False"
                    - Unknown
                    type: string
                  type:
                    description: Type is the type of condition for this resource
                    enum:
                    - Initializing
                    - Completed
                    - Failed
                    type: string
                required:
                - status
                - type
                type: object
              type: array
            passwordKeyId:
              description: PasswordKeyId is the key ID for the latest valid password
                credential
              type: string
            synchronizationHash:
              description: SynchronizationHash is the hash of the AzureAdCredential
                object
              type: string
            synchronizationTime:
              description: SynchronizationTime is the time when the resource completed
                synchronization
              format: date-time
              type: string
          required:
          - certificateKeyId
          - passwordKeyId
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
